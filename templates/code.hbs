/* Day {{day}} */

export const part1 = (input: string): number => {
  let result = 0;

  // ? here the input is split line by line
  input.split("\n").forEach((line) => { /* body of function */ });

  return result;
};

export const part2 = (input: string): number => {
  let result = 0;

  // ? here the input is split line by line
  input.split("\n").forEach((line) => { /* body of function */ });

  return result;
};
